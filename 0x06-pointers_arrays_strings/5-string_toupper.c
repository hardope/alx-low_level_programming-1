#include<stdio.h>
#include "main.h"
#include <string.h>
/**
* _strcat - Main Function
* @text: Parameter
*/
char *string_toupper(char *text);
{
	int i;

	for (i = 0; i < strlen(*text); i++)
	{
		if (i > 64 || i < 91)
		
	}
}
